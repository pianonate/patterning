package patterning.pattern

interface PerformanceTestable {
    // currently we're just indicating that the pattern will support a
    // running mode of Testing and that we will handle the test appropriately
}